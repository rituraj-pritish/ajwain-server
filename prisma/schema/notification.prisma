model Notification {
  id    Int   @id  @default(autoincrement())
  userId    Int
  status    NotificationStatus    @default(UNREAD)
  boardId   Int
  boardName   String
  workspaceId   Int
  workspaceName   String
  taskId    Int
  taskTitle    String
}